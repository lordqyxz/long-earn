---
description: 轻量任务主动分派 Composer 子代理；可并发时并行执行
alwaysApply: true
---

# 轻量任务 → Composer 自动分派

发现**轻量/机械**子任务时，**主动**用 Task 工具分派，不要自己硬扛。

## 模型

- 默认：`model: "composer-2.5"`
- 极短（单文件 lint 修、改一行、跑一条命令）：`composer-2.5-fast`
- 架构取舍、根因不明、跨模块设计、用户点名主模型：主代理自己做

## 何时分派（满足 ≥2 条）

- 范围清晰、验收可写进 prompt（ruff 清零、补测试、改 N 个明确文件）
- 不依赖本轮未写入的对话推理（可自包含）
- 预计 ≤15 分钟或 ≤3 文件的机械改动
- 主代理正在做编排/调研，子任务可并行

**典型**：ruff/format 修复、补进度日志、抽小函数消复杂度、补单测、跑指定 pytest、改 docstring/注释、按已定方案改配置

**不要分派**：需求含糊、要读大量上下文才能定方案、安全/数据铁律相关、需要连续多轮与用户对齐

## 并发

- 无共享写文件 / 无依赖 → **同一消息里多个 Task 并行**
- 有依赖（A 产出 B 消费）→ 串行，或一个子代理内完成
- 最多同时 3 个 Composer 任务；避免多人改同一文件

## Prompt 必须自包含

含：仓库路径、目标文件、具体改动、验收命令、`required_permissions: ["all"]`（本机 Windows sandbox 不可用）、**禁止 commit**（除非用户已要求）

## 主代理职责

分派后整合结果、处理失败重试/收回自做、只向用户报告结论与剩余风险；不把「已分派」当成完成。
